PRAGMA foreign_keys = off;
BEGIN TRANSACTION;

CREATE TABLE IF NOT EXISTS drzava (id INTEGER PRIMARY KEY, naziv TEXT, glavni_grad INTEGER REFERENCES grad (id));

CREATE TABLE IF NOT EXISTS grad (id INTEGER PRIMARY KEY, naziv TEXT, broj_stanovnika INTEGER, drzava INTEGER REFERENCES drzava (id) ON DELETE CASCADE);

COMMIT TRANSACTION;
PRAGMA foreign_keys = on;